# tor-rotation-ipproxy

🌐 Tor IP Rotator API (GitHub Codespaces)
ระบบ API สำหรับส่งข้อมูลผ่านเครือข่าย Tor พร้อมฟีเจอร์ Auto-Rotate IP (เปลี่ยน IP ทุกครั้งที่มีการเรียกใช้งาน) ออกแบบมาเพื่อใช้บน GitHub Codespaces หรือ Ubuntu

🛠 สิ่งที่ระบบนี้ทำได้
1. รับ Request ผ่านพอร์ต 8000
2. สั่งการไปยัง Tor Control Port (9051) เพื่อขอวงจรใหม่ (New Identity)
3. หน่วงเวลาเพื่อให้ IP ใหม่พร้อมใช้งาน
4. ส่งข้อมูลไปยัง URL เป้าหมายผ่าน Proxy SOCKS5 (9050)
5. คืนค่าผลลัพธ์พร้อมเลข IP ใหม่ที่ได้รับ

🚀 ขั้นตอนการติดตั้ง (Setup)
1. ติดตั้ง Tor และเครื่องมือที่จำเป็น
รันคำสั่งนี้ใน Terminal ของ Codespace:

Bash
sudo apt update && sudo apt install tor netcat-openbsd -y

2. ตั้งค่า Tor Control Port
เพื่อให้สคริปต์ของเราสั่งเปลี่ยน IP ได้ ต้องเปิดพอร์ตควบคุมของ Tor ก่อน:

เปิดไฟล์คอนฟิก: sudo nano /etc/tor/torrc

เพิ่มบรรทัดเหล่านี้ลงไปท้ายไฟล์:

Plaintext

ControlPort 9051
CookieAuthentication 0
บันทึก (Ctrl+O, Enter) และออก (Ctrl+X)

รีสตาร์ทบริการ Tor:

Bash

sudo service tor restart
3. เตรียมโปรเจกต์ Node.js
สร้างไฟล์ package.json หรือติดตั้ง Library ที่จำเป็น:

Bash

npm install express axios socks-proxy-agent
📄 โค้ดระบบหลัก (app.js)
สร้างไฟล์ชื่อ app.js และใช้โค้ดชุดนี้ที่ผ่านการปรับจูนเรื่อง Queue และ IP Rotation แล้ว:

JavaScript

const express = require('express');
const net = require('net');
const axios = require('axios');
const { SocksProxyAgent } = require('socks-proxy-agent');

const app = express();
const agent = new SocksProxyAgent('socks5h://127.0.0.1:9050');
let isRotating = false;

// ฟังก์ชันสั่งเปลี่ยน IP (NEWNYM)
const rotateIP = () => {
    return new Promise((resolve, reject) => {
        const client = net.createConnection({ port: 9051, host: '127.0.0.1' }, () => {
            client.write('AUTHENTICATE ""\n');
            client.write('signal NEWNYM\n');
            client.write('QUIT\n');
        });

        client.on('data', (data) => {
            if (data.toString().includes('250')) {
                console.log('🔄 Tor: Identity changed, waiting 5s for new circuit...');
                setTimeout(resolve, 5000); // รอ 5 วินาทีให้ IP ใหม่นิ่ง
            }
        });

        client.on('error', (err) => reject(err));
    });
};

app.get('/send-package', async (req, res) => {
    const { target_url } = req.query;
    
    // ระบบ Queue ป้องกันการสั่งเปลี่ยน IP ซ้อนกัน
    while (isRotating) { await new Promise(r => setTimeout(r, 500)); }

    try {
        isRotating = true;
        await rotateIP();
        
        const response = await axios.get(target_url, { 
            httpAgent: agent, 
            httpsAgent: agent,
            params: { _t: Date.now() } // ป้องกัน Cache
        });
        
        isRotating = false;
        console.log('✅ Success! Current IP:', response.data.IP || response.data);
        res.send({ status: 'Success', current_ip_data: response.data });
    } catch (error) {
        isRotating = false;
        res.status(500).send({ error: error.message });
    }
});

app.listen(8000, () => console.log('🚀 Tor API Server running on port 8000'));
📡 วิธีการใช้งาน (Usage)
1. รันเซิร์ฟเวอร์
Bash

node app.js
2. เปิดพอร์ตให้เป็น Public
ไปที่แท็บ Ports ใน VS Code ด้านล่าง

หาพอร์ต 8000

คลิกขวาที่ Visibility -> เปลี่ยนเป็น Public

3. เรียกใช้งานจากเครื่องภายนอก (Windows/Browser)
เรียกผ่าน URL ของ Codespace คุณ (เปลี่ยน URL เป็นของคุณ):

Plaintext

https://studious-winner-xxxx-8000.app.github.dev/send-package?target_url=https://check.torproject.org/api/ip
⚠️ ข้อควรระวัง
ความเร็ว: เครือข่าย Tor ช้ากว่าอินเทอร์เน็ตปกติ 3-5 เท่าเสมอ

การแบน: เว็บไซต์ใหญ่ๆ (Google, Cloudflare) มักบล็อก IP ของ Tor คุณอาจเจอ Error 403 ได้ในบางครั้ง

Security: เมื่อเปิดพอร์ตเป็น Public ทุกคนที่มี Link จะสามารถสั่งเปลี่ยน IP ของคุณได้ ไม่ควรใช้ในงานที่มีข้อมูลสำคัญโดยไม่มีการใส่รหัสผ่าน (Auth)

มีส่วนไหนที่อยากให้ผมอธิบายเพิ่มในไฟล์ README นี้ไหมครับ? เช่น วิธีการส่งแบบ POST หรือการตั้งค่ารหัสผ่านเข้า API?

